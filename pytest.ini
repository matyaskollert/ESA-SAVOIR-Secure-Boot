# Serial port of the STM32 board exposed in WSL2 (override via env var)
# e.g.  SERIAL_PORT=/dev/ttyACM0
#        PRIVATE_KEY=../uploader/keys/private_key.pem

[pytest]
testpaths = tests
addopts = -v --tb=short -p no:xdist
